# C/C++ with GCC
# Build your C/C++ project with GCC using make.
# Add steps that publish test results, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/c-cpp/gcc

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: sudo apt install libboost-all-dev python python-pip
- script: pip install setuptools nuitka
- script: pip3 install setuptools nuitka termcolor platform importlib 
- script: |
    make
  displayName: 'make'
